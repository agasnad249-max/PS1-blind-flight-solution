Blind Flight: Stark Logistics Rescue (PS1)

Agastya N
25123002

1) make sure you have python installed
2) a GPU is recommended (works otherwise too)

STEPS
1) download the repository
2) download the data from gaggle

kaggle competitions download -c the-blind-flight-synapse-drive-ps-1
unzip the-blind-flight-synapse-drive-ps-1.zip -d data/

3) Install the required libraries from requirements.txt
4) make sure the paths are set correctly in the code (The current paths I used have kaggle which does to work anymore)
5) Run the notebook
